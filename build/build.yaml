parameters:
  - name: printval
    default: 'print param'
stages:
  - stage: buildstage
    jobs:
      - job: stagejob
        steps:
          - bash: echo Stage Job stage/stage.yaml
          - bash: echo ${{parameters.printval}}